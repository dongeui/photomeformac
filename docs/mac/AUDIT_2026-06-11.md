# 2026-06-11 설계-구현 일치성 감사 및 수정 체크리스트

오늘 발견한 "설계와 구현이 어긋나거나 모호한 부분"의 캐노니컬 목록.
수정이 끝나면 체크하고 커밋 해시를 적는다. 세션이 끊겨도 여기서 재개한다.

## 이미 수정됨 (참고)

- [x] `/status`가 이벤트 루프에서 무거운 집계를 매 2초 실행 → 5초 TTL 캐시 + threadpool (`0ef7bc1`)
- [x] DirMtimeCache falsy 버그 — `or` 기본값 패턴이 빈(len 0) 캐시 인스턴스를 버려 scan_cache.json이 영영 생성 불가 (`6b59570`)
- [x] waiting_stable 파일이 있는 디렉터리가 캐시에 남아 영영 재관찰 안 되는 잠복 버그 (`6b59570`)
- [x] missing 파일이 디렉터리 변경 없이 부활하면 복구 불가 (`6b59570`)
- [x] 스캔 중단 시 캐시 전손 → 60초 체크포인트 저장 (`6b59570`)
- [x] 메뉴바/설정 페이지 수치 이원화 → `analyzed_current` 단일 지표 (`1e2e069`)
- [x] 설정 탭 다이어트 — 시작/진행은 메뉴바로 (`3e7125c`)
- [x] UI 변경에 가드 테스트 미동행 3건 정리 (`5c43f12`, `55424f3`)

## A. 동작 영향 — 수정 대상

- [x] **A-1** `app/api/scan.py` 동기 엔드포인트(`POST /scan`, `/scan/semantic-backfill`, `/scan/semantic-maintenance`)가 async 핸들러에서 스캔/분석 전체를 인라인 실행 → 이벤트 루프 블로킹. threadpool로 내린다. (완료)
- [x] **A-2** 스케줄러 주도 이미지 AI는 `scheduler.background_task`에만 기록되고 메뉴바는 `jobs.active_library_job`만 읽어 안 보임. 메뉴바가 background_task를 폴백으로 읽게 한다. (완료)
- [x] **A-5** 환경변수 캐노니컬을 PHOTOME_*로 통일(코드 선언부가 PHOTOMINE_*로 남아 있음; 조회는 이미 PHOTOME 우선이라 동작 변화 없음). PHOTOMINE_*은 레거시 별칭으로 유지. (완료 — opencv_zoo의 레거시-전용 조회 버그도 함께 수정)
- [x] **A-4** `performance.ai_summary`와 `semantic.coverage`가 같은 개념(remaining 등)을 따로 계산 + `status.py`의 `remaining_clip or remaining_for_clip` 폴백 혼용 → coverage 셈을 단일 소스로 사용. (완료 — 조사 결과 같은 카운트에서 파생이었고 폴백 혼용·공식 중복만 정리)
- [x] **A-3** `full_scan=True`의 실제 의미는 "missing 재처리 포함"뿐(dir 캐시는 항상 사용) — 명칭/문서 정리. `daily_full_scan_hour/minute` 설정은 어디서도 스케줄 계산에 쓰이지 않는 죽은 설정 → 제거. (완료 — full_scan 의미는 코드 주석으로 명시)
- [ ] **A-6** 슈퍼바이저 자동 재시작이 실제 상황에서 2회 미작동. Xcode 디버그 세션의 프로세스 suspend 추정 — 배포 빌드에서 재현되면 추적. (보류: 재현 불가)

## B. 죽은 코드 / UI 잔재

- [x] **B-7** `app/api/gallery.py:_render_metadata_panel` 죽은 코드. `1592536`에서 추가된 EXIF 패널이 `31e5a71`에서 호출부만 제거됨 — 갤러리 이미지 전용 방향과 일관되게 헬퍼 체인 8개 삭제. (완료)
- [x] **B-8** 설정 페이지 phase2(검색 개선) 카드: `display:none` 고정인데 마크업/JS/배지 전부 잔존 → 제거. (완료 — 진행 결과 패널은 동기화 카드 하나로 통합)

## C. 프로세스 패턴 (규칙으로 기록)

- UI를 줄이는 커밋은 가드 테스트/빌드 설정(Package.swift 등)을 같은 커밋에서 갱신한다.
- `or`로 기본값을 줄 때 `__len__`/컬렉션형 객체는 `if x is None` 패턴을 쓴다 (AGENTS.md 구현 원칙에 반영 완료).

## D. 문서 부패

- [x] **D-1** RELEASE_PLAN.md S1.5 "progress fraction 노출 확인 필요" 제약 문구 — 이미 완료된 항목, 삭제. (완료)
- [x] **D-2** RELEASE_PLAN.md 6/9 기록 "빌드 스크립트 weights hard-fail" 후속 완료 여부 확인·기록. (완료 — build_mac_app_bundle.sh hard-fail, isPortAvailable 폴백 모두 구현 확인)
- [x] **D-3** `PHOTOME_ENV_FILE` 주입 주체 서술이 실제 구현(scripts/mac_app_backend_env.py)과 어긋남 — 문서 수정. (완료)
